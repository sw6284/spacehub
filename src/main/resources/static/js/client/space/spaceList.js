document.addEventListener('DOMContentLoaded', function() {
    // 모든 카드에 대한 이벤트 리스너 추가 예시
    document.querySelectorAll('.card').forEach(function(card) {
        card.addEventListener('click', function() {
          
        });
    });
});
